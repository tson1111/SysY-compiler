#pragma once
#include <vector>
#include <string>
#include <fstream>
#include <map>
#include <stack>
#include "Symtab.h"

using TiggerList = std::vector<std::string>;

extern std::ostream* out;
extern std::map<int,std::string> token2str;


